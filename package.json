{
  "name": "react-native-yamap",
  "version": "1.0.20",
  "description": "Yandex.MapKit and Yandex.Geocoder for react-native",
  "main": "build/index.js",
  "scripts": {
    "clean": "rm -rf lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "yarn run clean && yarn build:modern && yarn build:legacy",
    "build:modern": "BABEL_ENV=modern babel src -d lib/modern/",
    "build:legacy": "BABEL_ENV=legacy babel src -d lib/legacy",
    "prepare": "rm -rf build && tsc",
  },
  "homepage": "https://github.com/volga-volga/react-native-yamap",
  "keywords": ["react-native", "mapkit", "yandex", "yamap", "polygon", "polyline", "marker", "geocoder"],
  "author": "Nikita Sirotkin",
  "license": "",
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^3.8.3",
    "@types/react": "*",
    "@types/react-native": "*"
  },
 
  "peerDependencies": {
    "react": "*",
    "react-native": "*"
  }
}
